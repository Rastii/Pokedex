server {
  listen 8080;
  server_name localhost;
  charset utf-8;

  location / {
    root sed_magic_1;
  }

  location /api {
    include uwsgi_params;
    uwsgi_pass unix:sed_magic_2/sock/app_uwsgi.sock;
  }
}