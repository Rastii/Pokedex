server {
  listen 8080;
  server_name localhost;
  charset utf-8;

  location / {
    root sed_will_automatically_fill_this_for_you;
  }

  location /api {
    include uwsgi_params;
    uwsgi_pass unix:/var/www/app/app_uwsgi.sock;
  }
}